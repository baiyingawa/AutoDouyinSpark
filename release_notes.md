## v2.9.1 (2026-09-04)

### 🐛 修复
- 修复自动续火花期间登录流程覆盖有效 Cookie，导致发送后误判为未登录的问题。
- 发送完成后不再触发额外的登录校验，避免有效登录状态被错误覆盖。
- 首页“今日状态”卡片不再随火花天数列表等高变化。
- 强制发送严格使用好友页勾选的目标，不再错误发送给全部好友。
- 历史记录采用原子写入，并在历史文件为空时从天数缓存恢复最近记录。

### ✨ 新增
- 好友批量添加后，抖音号与头像识别按最多 8 路并发执行。
- 左侧栏底部显示当前应用版本号。
- 设置页默认先显示“应用配置”，并将其排在邮箱配置之前。
